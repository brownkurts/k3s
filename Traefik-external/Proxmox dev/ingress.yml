apiVersion: v1
kind: Namespace
metadata:
  name: proxmox-dev # change me
---
kind: Service
apiVersion: v1
metadata:
  name: proxmox-dev # change me
  namespace: proxmox-dev # change me
spec:
  type: ExternalName
  ports:
    - name: https # change me
      port: 8006 # change me
      targetPort: 8006 # change me
  externalName: 192.168.200.205 # change me
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: proxmox-dev # change me
  namespace: proxmox-dev # change me
  annotations: 
    kubernetes.io/ingress.class: traefik-external
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`www.proxmox-dev.kbtech.org`) # change me
      kind: Rule
      services:
        - name: proxmox-dev # change me
          port: 8006 # change me
    - match: Host(`proxmox-dev.kbtech.org`) # change me
      kind: Rule
      services:
        - name: proxmox-dev # change me
          port: 8006 # change me
      middlewares:
        - name: default-headers
  tls:
    secretName: kbtech-tls # change me
---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: default-headers
  namespace: proxmox-dev # change me
spec:
  headers:
    browserXssFilter: true
    contentTypeNosniff: true
    forceSTSHeader: true
    stsIncludeSubdomains: true
    stsPreload: true
    stsSeconds: 15552000
    customFrameOptionsValue: SAMEORIGIN
    customRequestHeaders:
      X-Forwarded-Proto: https